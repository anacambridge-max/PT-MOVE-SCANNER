# Upstox Analytics Token (recommended for this read-only scanner)
# Generate from Upstox Developer Apps. Do NOT commit a real token.
UPSTOX_ANALYTICS_TOKEN=

# Optional fallback for a normal Upstox access token.
UPSTOX_ACCESS_TOKEN=
