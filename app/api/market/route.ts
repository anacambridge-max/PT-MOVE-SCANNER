import { NextResponse } from 'next/server'
import { gunzipSync } from 'node:zlib'

const V2='https://api.upstox.com/v2',V3='https://api.upstox.com/v3'
const MASTER='https://assets.upstox.com/market-quote/instruments/exchange/NSE.json.gz'
const MAX_SCAN=150,CONCURRENCY=5,TOP=30

type I={instrument_key:string;trading_symbol?:string;name?:string;segment?:string;instrument_type?:string;underlying_symbol?:string;underlying_key?:string;underlying_type?:string;expiry?:number|string}
type Q={instrument_token:string;symbol:string;last_price:number;volume?:number;net_change?:number;oi?:number}
type C=[string,number,number,number,number,number,number]
type D={date:string;open:number;high:number;low:number;close:number;volume:number}
const indexes=[['NIFTY 50','NIFTY 50'],['BANKNIFTY','BANK NIFTY'],['NIFTY MIDCAP 100','NIFTY MIDCAP'],['INDIA VIX','INDIA VIX']] as const
function key(x?:string|null){return x?.trim().replace('|',':')??''}
function date(offset=0){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const y=+p.find(x=>x.type==='year')!.value,m=+p.find(x=>x.type==='month')!.value,d=+p.find(x=>x.type==='day')!.value;return new Date(Date.UTC(y,m-1,d+offset)).toISOString().slice(0,10)}
function istHour(){return Number(new Intl.DateTimeFormat('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',hour12:false}).format(new Date()))}
function expiry(x?:number|string){if(typeof x==='string'){if(/^\d{4}-\d{2}-\d{2}$/.test(x))return x;const t=Date.parse(x);return Number.isFinite(t)?new Date(t).toISOString().slice(0,10):''}if(typeof x==='number'&&Number.isFinite(x))return new Date(x<1e12?x*1000:x).toISOString().slice(0,10);return ''}
function avg(a:number[]){return a.length?a.reduce((x,y)=>x+y,0)/a.length:0}
function dayOf(c:C){return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(c[0]))}
function daily(cs:C[]):D[]{const m=new Map<string,D>();for(const c of cs){const d=dayOf(c),x=m.get(d),o=+c[1],h=+c[2],l=+c[3],cl=+c[4],v=+c[5]||0;if(!x)m.set(d,{date:d,open:o,high:h,low:l,close:cl,volume:v});else{x.high=Math.max(x.high,h);x.low=Math.min(x.low,l);x.close=cl;x.volume+=v}}return Array.from(m.values()).sort((a,b)=>a.date.localeCompare(b.date))}
async function upstoxJson(url:string,t:string,label:string){let last=0;for(let attempt=0;attempt<4;attempt++){const r=await fetch(url,{headers:{Accept:'application/json',Authorization:`Bearer ${t}`},cache:'no-store'});if(r.ok)return await r.json();last=r.status;if(r.status!==429)throw Error(`${label} failed: ${r.status}`);const retry=Number(r.headers.get('retry-after')||0);await new Promise(resolve=>setTimeout(resolve,retry>0?Math.min(retry*1000,3500):500*(attempt+1)))}throw Error(`${label} failed: ${last} after retries`)}
async function fno(){const r=await fetch(MASTER,{cache:'no-store'});if(!r.ok)throw Error(`Instrument master failed: ${r.status}`);const b=Buffer.from(await r.arrayBuffer());let s='';try{s=gunzipSync(b).toString()}catch{s=b.toString()}const a=JSON.parse(s) as I[],today=date();const v=a.filter(x=>x.segment==='NSE_FO'&&x.instrument_type==='FUT'&&x.underlying_type==='EQUITY'&&x.underlying_key&&x.underlying_symbol&&expiry(x.expiry)>=today);const ex=Array.from(new Set(v.map(x=>expiry(x.expiry)).filter(Boolean))).sort()[0];const m=new Map<string,I>();for(const x of v.filter(x=>expiry(x.expiry)===ex))m.set(x.underlying_symbol!.toUpperCase(),x);return{stocks:Array.from(m.values()),expiry:ex??null,universeCount:m.size}}
async function search(q:string,t:string){const u=new URL(`${V2}/instruments/search`);u.searchParams.set('query',q);u.searchParams.set('exchanges','NSE');u.searchParams.set('segments','INDEX');u.searchParams.set('records','10');return((await upstoxJson(u.toString(),t,'Index search')).data??[] as I[]).find(x=>x.trading_symbol?.toUpperCase()===q.toUpperCase()||x.name?.toUpperCase()===q.toUpperCase())}
async function quotes(keys:string[],t:string){const out:Record<string,Q>={};for(let i=0;i<keys.length;i+=400){const u=new URL(`${V2}/market-quote/quotes`);u.searchParams.set('instrument_key',keys.slice(i,i+400).join(','));const d=(await upstoxJson(u.toString(),t,'Quotes')).data??{};for(const [k,q] of Object.entries(d) as [string,Q][])out[key(k)]=q,out[key(q.instrument_token)]=q}return out}
async function intraday5m(k:string,t:string){const d=await upstoxJson(`${V3}/historical-candle/intraday/${encodeURIComponent(k)}/minutes/5`,t,'Intraday 5m');return(d.data?.candles??[]) as C[]}
async function prevDaily(k:string,t:string){const d=await upstoxJson(`${V3}/historical-candle/${encodeURIComponent(k)}/days/1/${date(-1)}/${date(-7)}`,t,'Previous daily');return(d.data?.candles??[]) as C[]}
function t1(ds:D[]){if(ds.length<5)return null;const i=ds.length-1,c=ds[i],p=ds[i-1],cl=ds.map(x=>x.close);const ranges=ds.slice(-8).map(x=>x.high-x.low),r=c.high-c.low;return{bias:(c.close>p.close?'LONG':'SHORT') as 'LONG'|'SHORT',nr4:r<=Math.min(...ranges.slice(-4)),nr7:r<=Math.min(...ranges.slice(-7))}}
function trigger(session:C[],prev:D){if(session.length<21)return null;const candles=[...session].sort((a,b)=>new Date(a[0]).getTime()-new Date(b[0]).getTime()),opening=candles.slice(0,3),openingHigh=Math.max(...opening.map(x=>+x[2])),openingLow=Math.min(...opening.map(x=>+x[3]));let best:any=null;for(let i=20;i<candles.length;i++){const c=candles[i],v=+c[5]||0,sma=avg(candles.slice(i-20,i).map(x=>+x[5]||0)),vr=sma?v/sma:0;if(vr<2.5)continue;const h=+c[2],lo=+c[3],cl=+c[4],pdh=h>prev.high,pdl=lo<prev.low;if(!pdh&&!pdl)continue;const direction=(pdh?'LONG':'SHORT') as 'LONG'|'SHORT',orb=direction==='LONG'?cl>openingHigh:cl<openingLow;const hit={vr,pdh,pdl,orb,direction,candleTime:c[0]};if(!best||((orb?10:0)+vr)>((best.orb?10:0)+best.vr))best=hit}return best}
async function map<T,R>(a:T[],n:number,fn:(x:T)=>Promise<R>){const out:R[]=new Array(a.length);let i=0;async function run(){while(true){const j=i++;if(j>=a.length)return;out[j]=await fn(a[j])}}await Promise.all(Array.from({length:Math.min(n,a.length)},run));return out}
export async function GET(){const token=process.env.UPSTOX_ANALYTICS_TOKEN;if(!token)return NextResponse.json({ok:false,error:'UPSTOX_ANALYTICS_TOKEN is not configured'},{status:500});try{const[{stocks,expiry:nearExpiry,universeCount},ix]=await Promise.all([fno(),Promise.all(indexes.map(async([q,label])=>({label,instrument:await search(q,token)})))]);if(!stocks.length)throw Error('NSE F&O universe is empty');const keys=stocks.map(x=>x.underlying_key!).concat(ix.map(x=>x.instrument?.instrument_key).filter(Boolean) as string[]),qs=await quotes(keys,token);const nifty=ix.find(x=>x.label==='NIFTY 50')?.instrument?.instrument_key,nq=nifty?qs[key(nifty)]:undefined,niftyChange=Number(nq?.net_change??0);const pool=stocks.map(item=>({item,key:key(item.underlying_key),quote:qs[key(item.underlying_key)]})).filter((x):x is {item:I;key:string;quote:Q}=>Boolean(x.quote&&x.quote.last_price>0)).sort((a,b)=>Math.abs(b.quote.net_change??0)-Math.abs(a.quote.net_change??0)||(b.quote.volume??0)-(a.quote.volume??0)).slice(0,MAX_SCAN);const marketHours=istHour()>=9&&istHour()<16,marketDirection=niftyChange>0.15?'LONG':niftyChange<-0.15?'SHORT':'NEUTRAL';let intradaySuccess=0,intradayErrors=0,triggerCount=0;const rows=await map(pool,CONCURRENCY,async({item,key,quote})=>{try{const [ics,dcs]=await Promise.all([intraday5m(key,token),prevDaily(key,token)]);const session=ics.filter(c=>dayOf(c)===date());const ds=daily(dcs);const prev=ds.filter(x=>x.date<date()).at(-1);if(!session.length||!prev){intradayErrors++;return null}intradaySuccess++;const f=trigger(session,prev);if(!f)return null;triggerCount++;const change=Number(quote.net_change??0),rsAligned=f.direction==='LONG'?change>niftyChange+0.25:change<niftyChange-0.25,marketAligned=marketDirection==='NEUTRAL'||f.direction===marketDirection;const prev2=ds.at(-2),ranges=[prev2,prev].filter(Boolean).map(x=>(x as D).high-(x as D).low),pr=prev.high-prev.low,nr4=ranges.length>=2&&pr<=Math.min(...ranges),nr7=ds.length>=7&&pr<=Math.min(...ds.slice(-7).map(x=>x.high-x.low));let score=55+Math.min(18,f.vr*5)+(f.orb?7:0)+(rsAligned?7:0)+(marketAligned?5:0)+(nr4?4:0)+(nr7?4:0);score=Math.min(99,Math.round(score));const confirmation=['EXT VOL',f.orb?'ORB':'',rsAligned?'RS':'',marketAligned?'MKT':''].filter(Boolean).join(' ');return{rank:0,symbol:item.underlying_symbol!.toUpperCase(),name:item.name||item.trading_symbol||item.underlying_symbol!,sector:'F&O',score,bias:f.direction,change,volume:(quote.volume??0).toLocaleString('en-IN'),rs:rsAligned?'Strong':'Neutral',setup:`${confirmation} + ${f.pdh?'PDH BREAK':'PDL BREAK'}${nr4?' + NR4':''}${nr7?' + NR7':''}`,confidence:score>=90?'A+':score>=80?'A':score>=70?'B+':'B',lastPrice:quote.last_price,oi:quote.oi??null,rvol:+f.vr.toFixed(2),rangePct:0,nr4,nr7,pdBreak:f.pdh?'PDH':'PDL',t1Date:date(),filterPass:true,filterLabel:'LIVE NSE F&O + current-day V3 intraday 5m + previous-day PDH/PDL + extreme volume ≥2.5× previous 20 candles',filterDate:date(),signalTime:f.candleTime,marketDirection,marketAligned,openingRangeBreak:f.orb,extremeVolume:true}}catch{intradayErrors++;return null}});const candidates=rows.filter((x):x is NonNullable<typeof x>=>Boolean(x)).sort((a,b)=>b.score-a.score||Math.abs(b.change)-Math.abs(a.change)).slice(0,TOP).map((x,i)=>({...x,rank:i+1}));const indexesOut=ix.map(x=>{const q=qs[key(x.instrument?.instrument_key)],last=q?.last_price??null,net=q?.net_change??null;return{title:x.label,value:last,change:last&&net!=null?net/(last-net)*100:null}});return NextResponse.json({ok:true,source:'UPSTOX LIVE + V3 INTRADAY 5M',timestamp:new Date().toISOString(),universeCount,scanned:pool.length,expiry:nearExpiry,diagnostics:{intradaySuccess,intradayErrors,triggerCount,candidates:candidates.length,marketHours,marketDirection,niftyChange},filter:{universe:'NSE F&O stock futures — 150 active movers scanned',fiveMinuteVolume:'Current-day 5m trigger volume ≥ 2.5 × previous 20-candle SMA — mandatory',breakout:'5m High > Previous Day High for LONG OR 5m Low < Previous Day Low for SHORT — mandatory',openingRange:'Breakout candle close above first 15m high / below first 15m low gets confirmation bonus',relativeStrength:'LONG must outperform NIFTY 50 by 0.25%; SHORT must underperform NIFTY 50 by 0.25% as confirmation',marketDirection:'NIFTY 50 direction used as confirmation, not a hard blocker',structure:'Previous-day NR4/NR7 confirmation',price:'No artificial price filter'},candidates,indexes:indexesOut})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Market scan failed'},{status:500})}}
